#!/usr/bin/env bash
# commit-msg — the markdown budget guard (.claude/hooks/md-ratio.sh). Enable per clone: git config core.hooksPath .githooks
exec bash "$(git rev-parse --show-toplevel)/.claude/hooks/md-ratio.sh" commit "$1"
