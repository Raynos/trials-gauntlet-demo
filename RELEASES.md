# Releases

Pinned, permanent builds. Each entry is a git tag plus a Vercel alias that points at
that exact deployment forever (production `trials-gauntlet-demo.vercel.app` moves on).

| version | date (UTC) | commit | URL | notes |
|---|---|---|---|---|
| **v0.1.0** | 2026-09-14 | `94ecb43` | https://trials-gauntlet-v0-1-0.vercel.app | The build before the mega build. 15 tracks × 5 biomes, deterministic physics (10 rounds), console front end, real loader, glTF + procedural hero, iOS Safari touch/rotate, PB ghost. Strangers r2: b1 1.5 / b2 3 / e1 6 / b3 4. Blind critic: reference 6/6. |
| **v0.2.0** | 2026-09-15 | `b52dfd0` | https://trials-gauntlet-v0-2-0.vercel.app | **The mega build.** Physics v2 (R6, tag `physics-v2-final`: rider rigid body on a pose servo, brush tyre, Rookie/Pro rows, 157/157 tests, no masked rows), 15 tracks + **5 biome playgrounds**, Broadcast menu, odometer loader with the typed boot plan, G touch strip under the touch-navigation invariant (5184-tap grid, 0 ghosts), 60-cap quality governor + the phone-high tier (b1 205 → 123 draws, 6.57 → 1.59 Mpx), **level reviewer** (REVIEW tab: six segments, notes, Copy review), local leaderboard, `?bench=1` device instrument, WebKit hero gate, ship-gate G11, harness worker pools (reflex 67 → 21 s, e2e 2 327 → 564 s). Trailer + montage: https://trials-gauntlet-media.vercel.app/#v0-2-0. **Mission lines** (`docs/mission.md`): §1 wowed — 38-pair blind battery **ours 5 / 36** (0 of the 31 clean pairs; tells: camera 18, no settle 14, statue rider 9); §2 picture — blind critic r3 2 / 6, battery world 1 / 8; §3 hero — rider tells on 9 / 36 pairs, WebKit drift 2.1 mm; §4 60 fps on a phone — device report #1: low **59.5 fps** at cap 60 (JS 2.1 ms), phone-high modelled 8.8 ms, governor climbs; §5 sound — audio A/B r2 **2 / 6** (the engine loop named 4 / 4); §6 fun — strangers beginner–medium in band, hard on the R6 Pro h1 8 / h3 10 under band, x1 34.5 in band, h2 / x3 one DNF each; the user cleared 6 / 15 on the phone. Gate 22 / 26 under loadavg 65 (the four misses are SwiftShader timing rows + reflex band). |
| **v0.2.1** | 2026-09-16 | `c7a82e8` | https://trials-gauntlet-v0-2-1.vercel.app | **The merged-hero build.** Astra's branch on main: the AAA hero with five outfits, the garage / model catalog and the img2 rider experiment (Broadcast main + pause menus carry the rider rows); **physics R7–R9** — the rider is a body the game exports (`riderBody`, R7), sits on the bike behind seat / tank / reach limits with a thrown-rider fault and a brake brace (R8), and rides the asset's swingarm arc and fork line with the 0.10 m elbow stop and the Rookie brake lift control (R9; arc mismatch p99 26.6 → 0.43 mm, the mass frame rejected on hop 0.596 → 0.481); **render r15** — ResourceRetirement through the async link, context lost / restored, the five playgrounds drawing every set piece (stale programs −16–17 %, hero-webkit 0.00 mm); **tracks r11** — h2 / h3 / x3 re-authored from the stranger sinks (12 m platforms, 12 m brake zones, 8 m hop ledge). Evidence at the pin: 842 / 842 tests, 47 / 47 goldens re-searched at 1 attempt and browser-proved, D1–D8 9 / 9, gate **27 / 30** on the clean export (the three misses are the SwiftShader timing rows), reflex 9 / 9 clears, strangers n = 2 on b1–e3 12 / 12 cleared with e2 back in band; critic r4 2 / 12 (statue tells gone; landing punch + camera remain). The live pin replays the b1 Pro golden bit-equal (37.96666666666667, `f0549ee508d870ed`). No re-cut: the v0.2.0 trailer and montage stand. |
| **v0.2.2** | 2026-09-16 | `d8d5886` | https://trials-gauntlet-v0-2-2.vercel.app | **The garage build.** The garage is a model explorer (mockups B tool wall × E shutter door at BE3 dusk): hero unobstructed at 61 % of the viewport (was 92 % under the cards), drag to rotate / pinch to zoom (`CameraOverride 'orbit'`), a procedural set (shutter, dusk backdrop, pegboard, bench, tyre stacks; 17 draws) with a sodium key and a wet floor over a mirrored world + mirrored hero; the rider-model and outfit selectors live only in the garage (gone from the main and pause menus). The **level select** is six isometric night dioramas in one snap scroller (round-3 A3b lit like A3e): one scroll axis, 0 px vertical overflow, B1 in 2 taps. The **review inbox**: `?review=1` → ✎ NOTE on the HUD, password once, JPEG + repro state to a Vercel function + Blob, `pnpm inbox:pull`, `/drain-inbox`. The **img2 rider** no longer blacks the frame (uv-less anisotropy → NaN; the bloom high-pass guards NaN). Process: `docs/tasks/ASKS.md`, `project/human-in-the-loop/QUEUE.md`, the session brief, the 80/20 markdown guard. Evidence at the pin: 889 / 889 on the clean export, gate **27 / 30** (the three SwiftShader timing rows; boot ready 81 ms standalone), determinism 9 / 9, touch e2e front 471 / 471, the live pin replays the b1 Pro golden bit-equal (`37.96666666666667`, `f0549ee508d870ed`); played clips in `docs/evidence/garage-rebuild/` and `docs/evidence/level-select/`. No media re-cut. |

Release media (the trailer, the 15 s cut, the contact sheet, the cut list and the build montage of each pin) is kept
permanently in `project/releases/<version>/` and served at **https://trials-gauntlet-media.vercel.app/** (`pnpm media:deploy`).

To pin a new one (the v0.2.1 recipe): tag, then `git archive <tag> | tar -x -C <clean dir>`, `pnpm install --frozen-lockfile`,
`vercel link --yes --project trials-gauntlet-demo` there (never deploy from `dist/` — the Vercel project's preset is Vite, so a
bare `dist` upload tries to run `vite build` on the server and fails), `VERCEL_GIT_COMMIT_SHA=<sha> vercel build --prod`
(the build refuses to stamp without a sha, and an export has no `.git`), `vercel deploy --prebuilt --prod --yes`, then
`vercel alias set <deployment-url> trials-gauntlet-v<maj>-<min>-<patch>.vercel.app`
(deployment protection is off for this project so aliases are public). Run `pnpm harness:gate --quick` on the export first.
