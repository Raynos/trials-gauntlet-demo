<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="rockhop_cream">#EFE3C8</color>
    <color name="rockhop_teal">#0F5C63</color>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/rockhop_teal</item>
        <item name="colorPrimaryDark">@color/rockhop_teal</item>
        <item name="colorAccent">@color/rockhop_teal</item>
    </style>

    <!-- The game's window: no action bar, full screen, drawn under the display cutout on its short edges. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/rockhop_cream</item>
    </style>

    <!-- Launch: Android 12+ draws the system splash from these (cream + the icon); older versions show the
         cream-and-wordmark window background (scripts/store-assets.mjs). -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/rockhop_cream</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
