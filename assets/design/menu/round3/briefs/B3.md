## VARIANT B3 — "Glass" (the jump full-bleed behind everything; the panel is a translucent charcoal slab the rider flies past)
Architecture: the Lobby split, but the picture is the WHOLE screen and the panel floats on it. The JUMP fills all 1536 x 708: dusk with alpenglow on the Tian Shan, the camera high and BEHIND-LEFT of the rider so the arc goes AWAY and to the right over a wide Nalati valley — the meadow rolling down in green folds with drifts of wildflowers, the dirt line snaking below, a cluster of yurts with a thread of smoke, dark spruce, the pink-lit snow peaks and a deep blue sky. The rider and bike are mid-frame, high, small enough that the whole landscape reads, big enough to be the subject; the rear wheel throws a comet of dirt. One VORTEX OIL course marker on a post below.
RIGHT ~40 %: a tall GLASS SLAB — charcoal at about 80 % opacity with a soft blur of the scene behind it, a thin amber top edge, its left edge a crisp vertical line, running from 40 px under the top edge to 40 px above the bottom edge (so the scene shows above and below it). On the slab, STACKED from the bottom, four big PILLS the slab's full width minus 24 px margins, each 130+ image px tall with 16 px gaps: PLAY at the bottom (amber, the tallest, ~180 image px, "PLAY ▸" in black condensed italic), then GARAGE (bike icon), REVIEW (clapperboard icon), SETTINGS (gear icon) — charcoal pills a shade lighter than the glass, near-white condensed italic, icon left. Above the stack the slab is empty glass except a small grey tracked-caps "CREDITS" at its top-right corner.
The rider's arc crosses BEHIND the slab's upper part, so the glass is visibly in front of the world. The badge plate + build stamp sit top-left over the sky, as always. No text anywhere else. No ticker. No chip. No card.
Output path: assets/design/menu/round3/B3-glass.png
