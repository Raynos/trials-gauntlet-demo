
## FRAME CHECK (this is a retry: the previous attempt drew the screen too tall)
The phone screen must be EXACTLY 1536 x 708 image px — a wide 19.5:9 strip — with 158 px of PURE BLACK (#000000, no gradient, no glow) above and 158 px below. Do not let the screen grow taller than 708 px; do not let the map or any glow bleed into the letterbox. Compose the UI for that wide strip.
