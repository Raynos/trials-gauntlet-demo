# Parent selection — supersedes candidate manifest

The experiment manifest in this folder is a recipe archive, not the installed package. Parent selected the R9 fitted riders and R10 corrected bike; see `../hero-r10-promotion.json` for exact selected source copies and final LOD replacement. R10 Street/Race surface experiments were rejected after played review. `ship-metal.json` and `asset-revalidation.json` record the final integrated baseline.

# Canonical promotion inspection — no mutations performed

Paths and behavior were inspected in `assets/blender/{rider_asset,bike_asset}.py`, `assets/blender/README.md`, `src/boot/model-catalog.ts` and `vite.config.ts`.

1. After parent acceptance, preserve source provenance and intentionally copy the exact packed R10 sources to `assets/blender/source/rider-street.blend`, `rider-race.blend`, and `bike.blend`. The two rider sources already exist; bike is absent at inspection. Do not run rider `seed --replace-source`: that regenerates builder geometry and loses these authored candidates. Exact copies preserve reviewed source hashes. The bike `adopt` command is available for a fresh destination but saves metadata and can change the file hash; if used, retain the new hash and export lineage rather than claiming it equals the reviewed packed source.
2. Publish all six accepted GLBs with their six `.source.json` reports to `public/models/`. If moving exact reviewed bytes, retain the original audit verbatim or explicitly record any canonical-path relabeling with the unchanged source/output hashes; do not imply a fresh export occurred. Alternatively export full/LOD from each canonical source through standard exporters and validate/review any changed output bytes. Their default model/texture/generated destinations are canonical. There is no separate handwritten model hash manifest to maintain.
3. Run `pnpm build`. Vite regenerates `src/render/hero/models.generated.ts` (SHA-256, byte counts and pair-hashed URLs) and `src/boot/plan.generated.ts` (boot byte totals), then emits the deployment load manifest and hashed GLBs. Do not manually edit the generated tables or use stale staged build manifests. Model catalog requires full/LOD pairs. Runtime uses explicit Street/Race URLs; legacy `rider.glb`/`rider-lod.glb` are separate existing assets and are not part of this six-file candidate manifest.
4. Verify published six files and canonical source hashes against this package or the explicitly recorded new export lineage. Run the relevant production model checks and parent-owned cold-boot/clear/crash/restart gate against the resulting build; reuse captured evidence only when its model byte proofs match exactly. Update authoring README/provenance and parent plan/evidence status to the actual accepted state in the round commit.

The inspection does not authorize or perform canonical changes. Large authoring sources are outside this small recipe snapshot; the promotion commit/storage must actually retain them for durable future editing.
