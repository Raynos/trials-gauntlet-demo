# Riding-pose qualification, 2026-09-22

At simulation fingerprint `207404d3`, [the 50-recording refresh](golden-refresh-207404d3.log) proves exact finishing Node/browser replay on all 26 pre-Rockhop courses. Forty-seven controls came from bot searches; the B2 Pro, B3 Rookie and E2 Rookie `bot-3` files are explicitly marked stranger-derived. The M2 Pro control is a new bot clear after the one-sided seat repair. Two new blind sessions per B1–E3 course completed on this same fingerprint. Their committed `harness/out/metrics/*.stranger.json` reports show 12/12 clears, median attempts of 1, 1.5, 2.5, 3, 3.5 and 1 in course order; Stairway is under-band.

The [six-second Rookie/Pro transition video](final-rookie-pro-side-by-side-silent.mp4) replays the same neutral → forward → neutral → back → neutral control on both bikes. It is 1920×540, 30 fps and has one H.264 video stream with **no audio stream**. The underlying recordings and rider probes are in `harness/out/pose-motion-review/`.

The [current-pose garment playback](current-pose-cloth-played.json.gz) reruns the committed Rookie/Pro lean-transition inputs through production physics and CPU skinning for all five outfits at full and LOD detail. All four pose/renderer source hashes stayed fixed during the run. Across 20 subjects, 16,800 ridden ticks and 1,700 sampled garment frames, no sampled rear-fender vertex fell more than 5 mm beneath the fender top; the minimum sampled clearance was 8.195 mm (Pro Street, 4.333 s). This directly rechecks the earlier under-fender defect after the one-sided seat repair. It does not certify every triangle or garment appearance between samples, and the recordings have no ragdoll frames.

The [current-pose thrown-rider playback](current-pose-thrown-cloth-played.json.gz) uses actual H2 impact inputs on the same stable four source hashes. Across 20 subjects it covers 24,820 riding ticks, 4,800 ragdoll ticks and 2,490 sampled pre-release garment frames. No sampled vertex exceeded the 5 mm fender-overlap inspection envelope; the deepest was 3.060 mm below the fender top at 5.75 s on Pro Street. Three sampled frames had shallow negative clearance. The audit skips garment geometry once the rider becomes a ragdoll, so visible release continuity rests on the separate runtime release test and played impact clips.

The [headless WebKit phone-geometry probe](current-pose-webkit-phone-geometry.json) passed on B1 through tick 700 at 874×330 CSS pixels and DPR 3. Both low/LOD and high/full hero paths had zero hand IK residual, zero accumulated bone drift, at most 0.5 px GPU silhouette overhang and no browser errors. This is a macOS WebKit compatibility check, not a physical iPhone performance result.

The subsequent asset-provenance scrub changed the GLB file hashes. An independent [before/after GLB comparison](model-extras-equivalence.json) confirms that all 14 rider/bike models retain byte-identical BIN chunks and identical parsed JSON after removing `extras`; the pose, mesh, skin and animation payloads measured above therefore did not change. Served URLs were updated separately.

The first [silent Metal gate](metal-gate-207404d3.log) reached 25/31. Cold boot, clear, crash, one-tick restart, offline play and camera framing passed. Three old physics pins were [deliberately repinned and re-proved](repin-207404d3.log) 6/6. Heap growth passed a quieter recheck (4.91 MB against 5 MB), while synchronized GPU readback remained unstable under concurrent load. All 12 [new Rockhop course recordings were stale](rockhop-stale-207404d3.log) on this pose physics and must be replaced after their layouts settle. These are open release gates, not accepted exceptions.
