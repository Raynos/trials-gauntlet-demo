# PERF backlog — the cuts `PERF.md` did not land

Opened 2026-09-15 when `project/archive/PERF.md` closed as a ledger (cuts #0, #1, #3, #4a, #4b and the matrix half of #5 landed; phone-high tier live; 60-cap governor live; bench + WebKit gate in the harness, now ship-gate G11 rows). Everything below is ranked by **factor ÷ effort** the way `PERF.md` §3.2 ranked the originals; the numbers quote the closed ledger's last state (b1, phone geometry 874×330 @ 3, `pnpm harness:bench --tiers high,medium,low --tracks b1,h3 --geoms phone`). The cost model is `PERF.md` §2: `ms ≈ 4.4·effMpx + 0.02·calls + 0.004·ktris + 1.0`.

Where the ledger left the phone-high frame on b1: **≈ 123 calls / 92 k tris / 1.59 raw (1.14 effective) Mpx / 5 passes / model 8.8 ms** (h3 150 / 113 k / 9.4 ms). The two terms that remain are fill (1.14 × 4.4 ≈ 5.0 ms — the canvas at DPR 1.5, already the floor for that tier) and draws (123 × 0.02 ≈ 2.5 ms). Every row below moves draws, traversal or the spikes; none moves the fill term without lowering DPR.

Method rules carried over (they are why the ledger is trustworthy): counts (calls / tris / Mpx / programs / re-acquisitions) and **interleaved in-page A/Bs** are the comparators — cross-run CPU ms on this shared host move ±25 % with load; a cut is pixel-neutral when the A/B still at t400/800/1200 differs by ≤ ~0.1 % of pixels with ≤ a handful beyond 8/255 (the baked-vs-instanced edge-crack class); every cut lands with `pnpm exec tsx harness/bench/det.ts` 15/15 on phone-high / medium / low, `pnpm harness:hero-webkit` drift ≤ 5 mm, and the WebKit tier ratio (`harness/bench/webkit.ts`) unchanged.

| rank | cut | what is left / why it was not landed | factor (term) | effort | the measurement that proves it |
|---|---|---|---|---|---|
| 1 | **#4 remainder — draw calls 123 → ≤ 100 on b1 phone-high** | (a) **chunk-range culling for the merged world**: an 80 m chunk's bounding sphere (r ≈ 45 m) is coarse — a riding frame draws 2–3 chunks of everything; cull merged chunk meshes by camera-x range (`visible = |chunkCentre − camX| < halfWidth + margin`) once per chunk change instead of per-frame sphere tests, and split the per-chunk bake by depth band (z < −12 far / z ≥ −12 near) so the far band can be dropped on low (~15 draws); (b) **gates + lamps + plaques** into the merged path — today's `gates.ts` builds per-gate meshes (~14 draws at a gate); (c) the hall's remaining single-material batches that stay instanced because their baked copy would exceed `MERGE_MAX_TRIS` (60 k) — raise the cap per tier or bake in two halves | −20 to −35 calls → −0.4 to −0.7 ms model | 2 iterations | `pnpm harness:bench … --tracks b1,h3`: calls median ≤ 100 (b1) / ≤ 120 (h3) on phone-high; in-page A/B ≤ 0.1 % px; det 15/15 |
| 2 | **#4c decal array** (the −6 the #4b design promised) | the hall's 12 art decals (2 posters 1.2×1.8, 4 signs 0.9×0.9, 6 graffiti 2.6×2.6 alpha-tested) are 12 materials with 12 distinct art bitmaps of **different sizes** — an array texture needs one layer size, so every decal is resampled to (say) 512² → **not pixel-neutral by construction** (judge by the clip, not the A/B); two arrays (opaque posters + signs → 1 material; alpha-tested graffiti → 1) under names `HIDE_LOW` still recognises (`props:decal:poster-merged:*`, the low tier hides decals by name); the `skinArray.ts` machinery (`applySkinArray`, `withSkinIndex`, `shrinkSkinArrays`) is reusable as is | −5 to −6 calls (b1 phone-high / medium; low hides decals) → −0.1 ms model | 1 iteration | A/B calls −5..−6; the blind critic's b1 clip shows the posters / signs unchanged at the riding zoom; det 15/15 |
| 3 | **#5 culling half — cached render lists** | `projectObject` still walks ~1 240 nodes per frame (traverse 0.16–0.18 ms host on phone-high ≈ 0.5–1 ms Safari); the matrix half landed (row 3). Keep the static world in its own `Group` with a pre-culled list per chunk; on chunk change toggle `visible` on the chunk groups only; `sortObjects = false` for the opaque world (pre-sorted by material at build); the hero rig's ~50 nodes stay dynamic | traverse 0.17 → ≤ 0.05 ms host (E shrinks ~0.5 ms on the phone) | 2 iterations | bench `trav` column ≤ 0.05 ms on b1 / h3 phone-high; A/B 0 px (visibility-only change); det 15/15 |
| 4 | **Program dispose** (device report #1 item 5) | programs accumulate across tier / track changes (`debugInfo().stalePrograms` 12 → 85 → 72 on the phone) since the r14 prune removal; three never releases a material's non-current programs. Add a `releaseStalePrograms()` after `applyQuality` / `setTrack` that calls `renderer.properties.get(mat).programs` → `programCache.releaseProgram` for every program ≠ `currentProgram`, then warms the current one (`compileAsync`). Astra's branch claims a full reclamation — merge candidate first | memory (each program ≈ 0.5–2 MB driver-side on Metal) and the next-compile stall on a tier flip; no steady-state ms | 1 iteration | `stalePrograms` 0 after every tier change in the garage e2e; `?bench=1` device report `programs` flat across the tier rows; no new `programsBefore` growth in `entryStats` |
| 5 | **Bloomer list** (phone-high emissive bloom) | the emissive-only pass renders the whole scene with `camera.layers = BLOOM_LAYER` — three traverses and layer-tests ~1 240 nodes to draw ~30 sources. Keep the sources `tagBloomers` finds in a flat list / their own `Group` and render that (or reuse #5's cached list) | post 0.12 ms → ~0.05 host on phone-high (the `post` column) | ½ iteration | bench `post` column on phone-high b1 / h3; A/B 0 px |
| 6 | **#9 programs ≤ 16 + warm the exact variants** | 39–47 programs on high; a rebuilt LOD hero compiled a second variant (round 14); the cold first entry is 23 s on the phone (`entryMs`, item 5 — Metal shader link + 760 ms texture gen). One material recipe per surface class, `renderer.compileAsync` against the real target state during the loader, `initTexture` during the loader | first 10 frames after GO ≤ 2× the p50; entry spikes 200–1 000 ms → < 50 | 2 iterations | `entryStats.compileMs` / `programs` in `debugInfo()`; the device report's `entryMs` warm row |
| 7 | **#6 triangles 92 k → 60 k** (phone-high) | hero LOD document already on low / medium; prop LOD by distance (instanced batches carry a far variant — the merged bake can emit the far variant for z < −12), the deck AO skirt dropped where baked AO exists (28 k transparent tris on b1, high only), spokes → blur card above 4 rad/s on all tiers | −0.13 ms model on the tris term, more on SwiftShader / tile GPUs' vertex stage | 2 iterations | bench `tris` median; clip judged for the far-row containers / drums |
| 8 | **#2 shadow-on-change (world cascade back on phone-high)** | phone-high runs the hero-only 512² map; a 1024² world cascade costs 4.6 ms by the model unless it re-renders only on chunk change (needs a second light or a custom depth pass — the reason it was re-scoped) | contact shadows for the world on phone-high at ≈ +0.3 ms amortised instead of +4.6 | 3 iterations | rtMpx on phone-high ≤ 1.9; e2 / m2 / x1 rows (moving casters mark the map dirty); m2 clip |
| 9 | **Conditional smear / chroma pass on phone-high** (deferred from #3) | only while `speed > 9 m/s` or a flash is live: render to RGBA8 and one quad does smear + chroma + bloom-add; below 9 m/s skipped | +1.0 ms model only when on; parity with desktop high's speed feel | 1 iteration | passes 5 → 6 only above 9 m/s in the bench's per-frame records; the blind critic's h1 clip |
| 10 | **#8 half-rate updates** | bloom every 2nd frame (reuse the mip), particle upload at 30, gate crowd uniforms at 30, melt / lamp light re-pick every 10 frames | bloom 0.15 → 0.08 Mpx/frame; other 0.1 → 0.05 ms | 1 iteration | bench `post` / `other` columns; A/B ≤ 1/255 on the bloom halo |
| 11 | **#10 allocations / GC sawtooth** | 105–219 KB/frame of churn (§4 profile) — most of it three's `getParameters` is gone with cut #0; what remains: `getState()` copy per frame, HUD strings, uniform cache copies. The game-side half is the parent's | worst frame 66 ms → p95 + 5 ms | 1 iteration | heap flat over 600 frames (bench `heap` column ≤ +0.1 MB); device report worst-frame row |
| 12 | **#7 KTX2 / Basis textures** | 48–50 MB resident on phone-high; UASTC → ASTC 6×6 on iOS is 4× smaller in memory and bandwidth, no mip generation at load. The art owner's pipeline | tex 48 → ~15 MB; level-entry upload spikes | 3 iterations (art pipeline) | bench `tex MB`; `entryStats.textureMs` |
| 13 | **#11 z-order occlusion** | 2.5D: a prop entirely behind the deck / hall wall at the same x below the camera's line to the deck edge is never seen — static per-chunk list at build + per-frame camera-y test | h3 −30 %, b1 −10 % calls; volumetric overdraw −30 % | 2 iterations | bench calls on h3; A/B 0 px (true occluders only) |
| 14 | **low at DPR 1.25** (§3.4 trade-off) | the r12 DPR ≤ 1.0 on low pixelated the crowd / banner cards on the phone; with the fixed cost gone low has room for +56 % pixels (+0.6 ms model) or an SDF path for the banners | sharper low; costs fill | 1 iteration | device report low row still ≥ 58 fps at cap 60; the user's phone read |
| 15 | **#12 OffscreenCanvas + worker render** | main-thread render 0 ms; GC never blocks a tap; the harness's synchronous captures need a same-thread path kept | ∞ on main-thread jank, 1× total | 5 iterations | device report worst-frame / input-latency rows |
| 16 | **#13 WebGPU renderer** | render bundles make a draw ≈ free; Safari 26 / iOS 26 ships WebGPU; post chain, custom materials, KHR variants all re-done behind a flag | 4× on the draw term (2.5 → 0.6 ms) | 8+ iterations | the same bench matrix on the WebGPU path |

## Requests to other owners the backlog depends on

- **hero (Astra)**: hero static parts by material (15 → 6 scene draws, 16 → 6 shadow draws) — `src/render/hero/**`, the perf owner does not touch it; the program-dispose merge from her branch.
- **parent / game**: the game-side half of #10 (a reused state object, HUD strings on change).
- **art owner**: #7 (KTX2), and one layer size for the hall decals if #4c is to be pixel-faithful (author the posters / signs at 512² so the array is a straight copy).
