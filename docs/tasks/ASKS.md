# The user's asks — one ledger, every request, its status, its evidence

Kept current by the parent at every commit and every time the user asks for something. Newest at the bottom of each
table; a row never leaves — it moves from **open** to **done** (with the commit / evidence) or **dropped** (with the
user's words). If it is not in here, it was not asked, or the parent forgot — say so.

Status: **open** (nobody on it) · **in flight** (owner named) · **needs pick** (waiting on the user) · **done** · **dropped**.

## 2026-09-16

| # | ask (the user's words, shortened) | status | owner / evidence |
|---|---|---|---|
| 1 | Deploy latest and make it officially **0.2.1** in the releases | **done** | tag `v0.2.1` = `c7a82e8`, pin `613f6bf`, https://trials-gauntlet-v0-2-1.vercel.app, `RELEASES.md` row; gate 27/30 on the export, live pin replays the b1 Pro golden bit-equal |
| 2 | **Selecting img2threejs is completely broken** (black scene, HUD alive) | **done** | `b06f9ed` — the generator's uv-less hair shell carried an anisotropic material → NaN → bloom spread it over the frame; adapter zeroes anisotropy on uv-less meshes, the bloom high-pass drops NaN/Inf; reproduced + verified headless on Chromium and WebKit; deployed to production |
| 3 | I need to actually **see the model in the garage** / the hero is **blocked by buttons** | **done** | `2c1cfe9` — hero 61 % of the viewport, 0 tappables over it (was 92 % covered); v0.2.2 |
| 4 | The garage should **look like a garage**, the hero **bigger**, a **model-explorer view I can rotate** | **done** | `2c1cfe9` — BE3 dusk shutter-door set, orbit camera, drag/pinch; clip in `docs/evidence/garage-rebuild/`; v0.2.2 |
| 5 | **No outfit / Classic-Blender-Img2 selectors on the main menu or pause menu** — all in the garage | **done** | `2c1cfe9`; experimental-rider e2e proves both menus carry no rows; v0.2.2 |
| 6 | **5 Codex image mockups of a new garage** | **done** | picked B UI × E set → BE3, built in v0.2.2; `assets/design/garage/` A pit box · B tool wall · C showroom spot · D workbench · E shutter door + contact sheet + SPEC (recommends C); sent to the user |
| 7 | Did we have 5 mockups of a new **level select**? | **done** | yes — `assets/design/tracks/` A–E (`3351dbb`), never picked; sent to the user |
| 8 | **What mockups are lying around not built** | **done** | tracks A–E (the only orphan); menu B, loading B, controls G, pause chosen direction are built; `hero-targets` are art targets, not UI |
| 9 | Show me all the level-select ones | **done** | sent contact sheet + A–E + current |
| 10 | **Delta image, 0.1.0 Blender hero vs 0.2.1** / 3 before-and-after shots of just the hero | **done** | `docs/evidence/hero-delta-v010-v021/`: rider ×3, bike ×3 (+ |diff| ×3), the three outfits; rider 11 718 → 44 734 tris + real head + 3 outfits, bike 29 356 → 29 940 tris (livery + parts only); verdict = HR-02 |
| 11 | **Build A biome map**; new mockups of **A+D**, **A+E**, and **A1 / A2 / A3** | **done** | mockups `a67512d`; build `a302e62` on the A3 line; `assets/design/tracks/round2/` A1 / A2 / A3 / AD / AE + contact sheet + SPEC (recommends A2 skeleton + AE road + AD shelf); sent; build = HD-01 |
| 12 | Are we keeping a **durable task list of all my asks**? | **done** | this file; linked from `docs/plans/README.md` and `AGENTS.md` |
| 13 | Which repos have the **session brief** / **human review** flow; we need that here too | **done** | found: `games/kami-kakushi` (origin, 372-line brief), `kami-kakushi2`, `house` (`.claude/hooks/session-brief.sh` + `project/human-in-the-loop/`), `vibe-demos/fe-shooter-prototype-fable` (54-line port + `QUEUE.md`); ported the light version here: `project/human-in-the-loop/QUEUE.md`, `.claude/hooks/session-brief.sh`, `.claude/settings.json` SessionStart |
| 14 | **Update AGENTS.md** with the asks system; document undocumented processes — terse, pointers only | **done** | six pointer bullets appended (asks, human queue, session brief, design rounds, pins/evidence/device, harness docs) |
| 15 | **Before-and-after videos** of the 0.1.0 vs 0.2.1 Blender models | **done** | `docs/evidence/hero-delta-v010-v021/{rider,bike}-turntable-before-after.mp4` (360°, 6 s, side by side); sent |
| 16 | Garage: **UI layout = B**, **set = E**; three new mockups combining them | **done** | `c3fc76f` `assets/design/garage/round2/` BE1–BE3 + SPEC; sent |
| 17 | **BE3 dusk** for the garage | **done** | `2c1cfe9`; draws down (WebKit medium 148 → 101), synced frame 3.6 → 2.3 ms |
| 18 | Process, run by me: AGENTS.md trimmed to 3 bullets; queue delete-on-decision; **80/20 markdown budget** with a commit guard (refuse > 40 % unless Design:/Docs:) | **done** | `d788414`; `.githooks/commit-msg` → `.claude/hooks/md-ratio.sh`; the brief prints the tree's share (13 % today) |
| 19 | Port from FF15: **in-game review inbox + drain skill** — Vercel function + Blob, **password entered once, kept in localStorage** | **done** | `70c5d47`, live on production (`?review=1` shows the ✎ NOTE pill; password in chat + scratch); `pnpm inbox:pull`, `/drain-inbox`; 874/874; HR-05 = the first real note |
| 20 | Hero art (HR-02): the user takes it to Codex themselves | **dropped** | user-owned; not an agent item here; `prototypes/hero-garage` stays the user's |
| 21 | **v0.2.2 when the garage lands**; **deploy on green**, review live on the phone | **done** | tag `v0.2.2` = `d8d5886`, https://trials-gauntlet-v0-2-2.vercel.app, production moved; HR-04 is the review |
| 22 | **5 variants of A3 isometric diorama** for the level select | **done** | `d01c42d` `assets/design/tracks/round3/` A3a–A3e + SPEC; sent |
| 23 | "Enough questions — **build autonomously**: new garage, new levels, all the little asks; leave the physics-library plan for another agent" | **done** | **all landed**: level select `a302e62`, inbox `70c5d47`, garage `2c1cfe9`; pinned **v0.2.2**; physics library untouched |
| 24 | Next after the garage = **level select**; keep all five outfits + three models; physics-library toggle off the main menu when built; stray `0` deleted | **done** | recorded; `0` removed in `d01c42d` |

| HG-01 | Full person + full bike, screenshot in Three.js; work breadth first, pause hair/beard depth | **in flight** | Codex hero owner: complete scene and six poses present; packaged runtime verified, screenshot/recording in captures/whole-package-round17; hair/beard paused, production plan remains open |

| HG-02 | Show latest images; prioritize the last 2% working allowance | **done** | Latest full/mobile Three.js images shown; identity assembly now takes priority over materials and collar repair |
| HG-03 | Put the saved curly hair and beard onto the full rider and show it | **done** | Actual full/portrait Three.js renders in captures/full-rider-saved-identity-a1; original saved head attached, neck fit provisional |
| HG-04 | What happened to local Hunyuan/TRELLIS generation? | **done** | Hunyuan generated rejected malformed mesh; TRELLIS no successful inference, DINOv3 blocked; both stopped, status relayed |

## Standing items the asks imply (parent's list, not the user's words)

- Production deploy after the garage round lands (the v0.2.1 pin stays where it is; production moves).
- `docs/plans/README.md` line "pinned v0.1.0" still names only v0.1.0 / v0.2.0 — update when that file is next quiet (another owner is editing it).
- The stray untracked file `0` in the repo root (a shell-redirect typo) — the user's call to delete.
| 25 | Garage on desktop (2000×1015, tier L): "look at this, it's so dumb" — rider corrupted, the set crude | **done** | rider `2c59c46`; set `8fee28b` (skyline card through the shutter, wet concrete, corner brackets, sodium key aimed at the hero), live; side-by-side vs BE3 in `docs/evidence/garage-rebuild/desktop-set-vs-be3.png`; desktop 2000×1015 is now a first-class geometry. Still short of the mockup: the skyline is a procedural card, the default view is 3/4 not side-on |
| 26 | "**Fix the colors**, do you see the black and the corruption?" | **done** | `2c59c46`, live — the rider's albedo atlas halved to 512 + mipmapped on `low` bled its black gutters and the race jersey into the hoodie; skinned meshes keep 1024 albedo. Bisected texture by texture (twins / bias / lights / depth / side all ruled out). Deploy needed `scripts/vercel-deref-func.py` (`7fd398e`) — the prebuilt upload stopped resolving pnpm symlinks this afternoon |
| 27 | "Rescue the subagents, resume and continue" (×2, after the lid-close) | **done** | garage owner + harness owner resumed with re-orientation messages; both had intact uncommitted work |

| HG-05 | Finish Blender art to highest achievable quality; flexible two-pass allowance, stop at 2%; Claude + Opus own integration | **in flight** | R33 art provisionally accepted; complete rider/bike, editable master, reproducible exports and motion evidence delivered; final visual approval, AAA sculpt quality and actual iPhone remain open; no game integration |
| 28 | (parent's item) harness debt after the three UI rounds — full e2e back to green | **done** | `6ad…`/`fa4b440`: 724/765 → **1249/1249**; expectations updated to the shipped UI, timing made robust, `app.quit()` hook added; CONTRACT/rendering/game docs carry `setGarageStage` + `orbit` + the trackMap pages |

| HG-06 | Upgrade all five existing rider outfits and five bike skins with remaining usage | **in flight** | R34 delivers five outfit exports and two canonical bike skins with recorded motion and editable masters; three further bike designs await identification. Final art/device approval open; no integration; hard stop at 2% |
| 29 | "Every time I hover over a card in the garage, the whole screen flashes gray and shows me different stats" / "Switching between rookie & pro definitely flashes the whole garage gray" | **done** | hover → `previewBike` → `game.setBike` did a full `loadTrack` under the stage (world torn down + rebuilt: probe showed `clearWorld` + `setTrack` ×1 and the stage's hidden meshes 575 → 389 on one hover). Menu phase now swaps livery + engine voice only; the physics row reloads on the next arm (`startRun` / `loadTrack`). e2e G4 (`front`) asserts 0 `setTrack` per swap and fails on the old build. The stats sheet following the hovered chip is the intended preview and stays |
| 30 | "Remove the classic model from the garage" | **done** | with 31: the rider-model row left the garage |
| 31 | "img2threejs is a disaster, remove that too" | **done** | Img2 chip gone → only Blender left → the whole rider-model row left the garage (rail: outfit, bike); a stored proc/img2 rider reads as Blender (`loadModelChoice`); `?rider=img2|proc` stays a harness override (experimental-rider e2e proves both); the img2 renderer path itself is still in the tree — say the word and it goes |
| 32 | "Garage doesn't need any keyboard controls, just mouse and esc" | **done** | `nav` / `confirm` and the Tab-follow preview are gone from the garage; the app routes only back / pause (Esc, gamepad B) to it; legend reads `Esc Back` / `B Back`. Note: a gamepad-only player can now only leave the garage — flag if that matters |
| 33 | "Home screen feels stale & old next to the garage & new level select — 5 new mockups of a remastered home screen in that style, via the codex cli img gen" | **done** | `assets/design/menu/round2/` — A Bay, B Lobby, C Pit wall, D World, E Title card + contact sheet + SPEC; the user asked for more of B (ask 36) |
| 34 | "The dioramas in the level select are individual islands / pages, left-right to switch — I prefer a continuous flow, one continuous beautiful world map of these biomes combined; start with 3 codex mockups" | **done** | `assets/design/tracks/round4/` — A Ribbon, B Chart, C Ascent + live-diorama paging captures + SPEC; the user picks |
| 35 | "You can't drive the bike backwards — the brake button just stops the bike, it doesn't reverse" | **done** | physics v2 R10: brake held 0.2 s at ≈ standstill (no throttle, rear grounded, not finished) creeps back — settles at 2.44 m/s Rookie / 2.93 Pro (vmax 2.5 / 3.0), brake from speed is still the brake (never reverses mid-stop), throttle overrides in one tick, release stops within 1 s; 47/47 goldens byte-identical (every brake-at-rest in them is the bot's 0.125 s quantum), D1–D8 9/9; `physics.md` "v2 status — R10"; `docs/evidence/physics-r10-reverse/` |
| 36 | "Can we break hero open work md into multiple smaller plans" → "yes I think splitting it out would be great" | **done** | `HERO_OPEN_WORK.md` → `project/archive/`; successors `docs/plans/RIDING_POSES.md`, `CHROMIUM_METAL_SHADER_INIT.md`, `HERO_ART_INTEGRATION.md`; README tracker rows + AGENTS pointer updated (this commit) |
| 37 | "/goal build CHROMIUM_METAL_SHADER_INIT.md until completion" | **done** | closed as non-repro: 18/18 clean unmodified Metal gate runs incl. the original failing build and pre-r15 (`docs/evidence/chromium-metal/`); plan archived; cold-cache residual declined by the user, recipe kept in the evidence README |

| HG-07 | Remaster the helmet | **in flight** | Open-face remaster delivered provisionally with24 recorded views and editable source; final user approval/Race scope open. Stopped sustained work at2% |
| 39 | "Main menu: more variants of B Lobby — larger GARAGE / PLAY / SETTINGS / REVIEW buttons; leak nothing about level, session, next up, cleared; no massive hero card — a cool midair jump in the not-yet-built Nalati grassland biome instead; 3 new variants" | **done** | `assets/design/menu/round3/` — B1 Plate, B2 Strip, B3 Glass (all leak-free, every action ≥ 72 px, Nalati jump); SPEC recommends B1; the user picks |
| 38 | "It's definitely C Ascent for the levels / world map. The world map can be panned, dragged and zoomed — no need to cram all 15 levels into a static view: show a subset in the viewport and move the map to see the rest, or load the viewport centred on the current level wherever it is on the map" | **done (interaction); art pass open** | one pannable / zoomable world: the six plates stacked as the C Ascent mountain (apron → Industrial over a quay → Canyon → Snow → Night City → Foundry, iso steps + cliff bands), one trail through all 15 pins in biome order, the hazard-tape barrier at the locked seam, a vertical altimeter with `n/m` + padlocks, drag with inertia / pinch / wheel zoom 0.2–2.0, opens on the current level with 4 pins in view; 0 overlaps / 0 targets < 44 px in 24 states on Chromium + WebKit; e2e front 745/745 · run 108/108 · desktop 188/188; `docs/evidence/level-select/round4/`. **Gap:** the seams are CSS strata, not the painted mountain of the mockup — a codex seam-plate art round is the next step (ask 44) |
| 40 | "Garage stats sheet only on click" (hover no longer previews) | **done** | a chip under the pointer only highlights (and the highlight returns to the chosen chip on leave); click commits bike + sheet + hero livery; `previewBike` left the garage contract |
| 41 | "Delete the img2 code, rip it out" | **done** | `img2Rider.ts` + test, the `?rider=img2` path, `ModelChoice` 'img2', `userData.img2Experiment`, the vite chunk, the eslint carve-out, `public/img2-experiment-assets.json` + 45 PBR maps, the generated `createStreetMustard.ts` factory and `.img2threejs/` gone — 7.87 MB of assets, −63.9 kB gz bundle (601.9 → 538.1); a stored `trials.riderModel = img2` still boots as the Blender rider (unit + e2e); the 51 deletions rode in `dc450e0`, the source edits in the ask-41 commit. Left for the hero owner: `assets/design/hero-targets/reconstruction/` (24 MB of img2threejs analysis the prototype still reads) |
| 42 | "Main menu B2, build it" | **done** | B2 Strip built: Nalati-jump strip under the two-line wordmark, four tiles GARAGE · REVIEW · SETTINGS · PLAY (amber, 1.6×) at 95 / 86 / 132 px on 932×430 / 844×390 / 1280×720, CREDITS a small word; the ticker, bike chip, up-next and every progress read deleted from the class (leak audit in the e2e on every menu visit); generated Nalati plate in the art pack (236 KB 2x / 98 KB 1x), the boot prefetch streams it; e2e front + desktop green; `docs/evidence/main-menu-b2/` (stills + played clip). Open: the fps meter still draws over the strip; wordmark + badge both read the name |
| 43 | "/goal build the OPUS HANDOFF and HERO ART INTEGRATION — all this new blender work needs integrating into the main game; delete the prototype when everything (assets, models, graphics, improved physics) is in main" | **done** | rounds `3af533e` (14 game-budget GLBs, family live, rider LOD on phone tiers), `9b09013` (hair shell v2, Street merge fix), `4f27f47` (delivery + masters + handoff into trunk), `45f2e71` (boot on the first-drawn pair, ship gate PASS), then the legacy family and `prototypes/hero-garage/` (1 436 tracked files, ~50 GB local → Trash) retired; "improved physics" = R10 `dc450e0` (the prototype held none). Your verdict on the hair, the licence call and the iPhone reading are HR-09 |
| 44 | (parent, from 38) the world map's seams: paint the C Ascent mountain — cliff bands, quay, massif, chairlift / waterfall dressing — as real plates instead of CSS strata | **done** | nine codex runs → five seam strips (quay, Industrial→Canyon, Canyon→Snow, Snow→Night City, Night City→Foundry), a tileable massif rock, chairlift / lit cabin / waterfall sprites; `assets/art/tiles.mjs` pipeline → `public/art/tiles/` (+695 KB lazy, never on the boot set), CSS strata kept as the fallback until the probes resolve; 24/24 states clean, e2e front 421/421 · run 100/100 · desktop 188/188; before/after + climb clip in `docs/evidence/level-select/round5/`. Left: the massif's straight right edge; the raw PNGs were committed with ask 69 (regenerate with `build/gen.sh`) |
| 45 | (asked) hide the fps meter on the main menu; badge plate → build stamp only under the wordmark | **done** | `6cb3cd4` — the meter hides while `screen === "menu"` (guarded by `menu-no-fps-meter` in the e2e and `meterHidden` in the stills audit), the plate carries only `build <sha> · <date>`; live |
| 46 | "Ask me" → keep the hair shell; clear the licences — deploy with attribution; a URL to play the garage + b1 on the iPhone | **done** | hair shell kept; Hero row on the Credits screen (`f2ea1c8`); **v0.3.0 pinned: https://trials-gauntlet-v0-3-0.vercel.app** (also production); HR-09 closed on the user's CC-BY reading; user accepted hero integration closure in ask 71 without a measured iPhone fps result |
| 56 | (pasted) `git push origin main` rejected — "pack exceeds maximum allowed size (2.00 GiB)"; first push of the repo | **done** | With the user's approval, removed only the 141 657 181 B `harness/out/trailer/trailer-v0.2.0.mp4` from rewritten history (the local master remains ignored). The rewritten HEAD tree matches the original except for that path; no reachable blob exceeds 100 MB. Uploaded the 345-commit history to GitHub through verified commit boundaries of at most 25 new commits, splitting the large art span around GitHub's 2 GiB pack cap; `origin/main` was then moved to the full rewritten tip. |
| 53 | "What the fuck is that world map … just dioramas linked on one page … complete shit. Use Codex CLI, make three new mockups of a beautiful world map — Final Fantasy VII Rebirth, Final Fantasy XV, Final Fantasy XVI, Black Desert Online. From scratch, built absolutely from scratch all the way up. A brand new world map" | **done** | `assets/design/worldmap/` — A painted terrain (Rebirth), B cartographer's sheet (XV / XVI), C satellite nodes (BDO), each at region + whole-world zoom, no diorama reference fed to the generator; contact sheet, briefs, SPEC with per-direction from-scratch estimates (~4 / 5 / 5.5 rounds). **The user picked A** → ask 54 |
| 48 | "The main menu is not designed for mobile — it needs to be resized: the bar buttons at the bottom need to be smaller, the hero image needs to be centered better, just needs to be fixed" (iPhone landscape screenshot, build cb9ab93) | **done** | `html.short` menu rules in `styles.ts`: one thin band (52 px tiles, icon beside the word, 44 px CREDITS row) so the strip keeps ~3/4 of the height, crop window at 22 % so the rider's helmet stays in frame, title 13 vh; desktop unchanged; phone tile floor 52 in `menu-stills` + the front flow (557/557); before/after in `docs/evidence/main-menu-b2/phone-ask48/` |
| 49 | "I give you approval to do the fix list from Astra's own handoff — cloth, hand, race helmet remaster, all those things; if you can figure that out, that'd be great" | **done** | from the masters via the pipeline: Race full-face helmet remaster, hands at 85 % of the delivered detail (the collapse had flattened them), Race collar/yoke lofted from the jersey's neckline, garment normals baked from the pre-decimation garments, hair shell v3 (temples rounded, colour on target); the bike proven identical to the delivery. Needs real sculpting in the masters, not pipeline: glove/thumb shapes, shoulder/elbow folds, groom density — `docs/evidence/hero-art/fixlist/*` |
| 50 | "Switching skins in the garage is really slow — some invisible mega loading bar; there shouldn't be any: one loading bar to start, everything preloaded up front; bake / pre-bake at build time if needed (we have a Vercel build step); it needs to be really fast and snappy — first click slow, then still sluggish" (iPhone garage shots: 60 fps → 51 fps / 102 ms on an outfit tap) | **done** | resident hero pool warmed in the one boot bar; 23 taps: install 11 ms median, +0 programs / +0 textures / 0 requests (`docs/evidence/hero-art/swap-r0` → `swap-r1`); root cause: dispose dropped the hero shader variants to refcount 0, relinked next frame |
| 51 | "Blend the garage prototype: fix the physics and anatomy — bone structure, anatomy, the hands. The standalone prototype's renders didn't have the fucked-up elbows; compare screenshots of the prototype garage and the in-game garage — bones, skeleton shape, position on the bike — the elbows were fixed there and in the deployed build they're fucked; maybe the skeleton fixes weren't copied over" | **done** | skeleton was identical; the driver was not — garage plays `sit_cruise` (equal to the prototype), level stances from `riderBody.drawn` with IK as a clamped correction: hands 0 cm every tick, elbows ≤ 168.5°, seated on the flat; `docs/evidence/hero-art/pose-compare`, `pose-r1` |
| 54 | "The Rebirth world map mockup looks fantastic — I want that, literally that, for the level selector. Build a new level selector from scratch. Not easy, but you can do it; work really hard on that" | **done — archived** | `ddad17f` + `125e490` built the painted continent, camera, markers, road, gate, fog and card; the diorama screen, data, tiles and e2e were deleted. `411697e` fixed Canyon opening framing. The user accepted closure in ask 71; actual iPhone pinch remains unmeasured. Plan: `project/archive/WORLD_MAP.md`. |
| 52 | "I don't think we imported the new version of the bike from the standalone garage — the bike in the standalone garage and in our garage look completely different; the hair and beard came over, what about the bike?" | **done** | delivered bike proven identical to the shipped one (≤ 0.034 sRGB/part); the garage now draws the authored bike on every tier and a stage-only cool lift makes the blue read (tank 0.56/0.60/0.80 vs neutral 0.50/0.57/0.80); `fixlist/bike-look` |
| 55 | (from the sibling session's harness) on WebKit at 874×330 the GARAGE tile tap did not open the garage within 10 s, twice, after a run (fell back to `app.goto`) — `docs/evidence/hero-art/swap-r0/swap-log.json` `garageEntry` | **queued** | parent; repro headless on WebKit after the world map's R1 |
| 60 | "Fable says we only updated the models in the garage, not in the levels — double check; the garage / skin-select fidelity must translate into the game: the exact same high-fidelity model in the main game" | **done** | `lodChoice`: authored rider + bike in level on every tier but `low`; phone-high 139 calls / 240 k tris / 9.7 ms; b1 phone-high clip shows `bike rider` (`swap-r1`) |
| 61 | (from the outside review the user relayed) "in-run you get a floating placeholder quad" — find it and kill it | **done** | the hall's `tarp` prop: an untextured `PlaneGeometry(3, 2.4)` hung 3–6 m under the roof with nothing holding it (industrial + foundry); now a draped textured sheet on chains, sharing the container skins' program (57 → 54 programs); the flat `hazardTape` sign became a printed board on posts; `docs/evidence/world/placeholder-quad/` |
| 62 | (same review) "cloned photo-cutout crowds" — replace with the earlier low-poly crowd or silhouettes | **done** | `src/render/world/crowd.ts`: 16 painted silhouette figures per track, 10 poses, shirt/skin/hair variety, night cast; still one instanced batch; photo sheet off (`CROWD_PHOTO_SHEET`), −119 KB boot; b1 / p1 / p2 gate clips in `docs/evidence/world/crowd/` (the biome-pair rubric scores motion, not spectators — unchanged) |
| 57 | (parent) ship-gate `boot.readyP50Ms` read 608 ms on the `a32d7e0` export (limit 300; was 155 on `cb9ab93`; runs 1888 / 608 / 107, loadavg 5.6) | **done — noise** | the next export (`125e490`) read back under the limit: gate 27/30, only the three SwiftShader rows |
| 58 | "I tried to play the game with no data, no internet, and it doesn't work — I have it saved to the home screen and it didn't load … after the first load I want to play it offline" → "load everything up front … second boot: all cache" → "the update toast always felt buggy" | **done** | worker registers first thing on the loading screen (`src/boot/sw.ts`), three caches that prune instead of wipe, build stamped by content hash; **one** online load now leaves 180 entries / 41.96 MB incl. all 14 models, and a cold start with the origin down plays: 0 requests, 0 bytes, the b1 golden bit-identical, the garage 10/10 outfit × livery with no network. The toast is deleted — a waiting build activates at the head of the loading screen; an update boot costs 39 829 B (0.10 %). Everything up front via `offlinePack` (DOWNLOAD 27.02 → 38.44 MB); `vercel.json` 1 y immutable models, 1 month art / fonts, versioned art URLs; iOS icons + 28 splash images; new `harness/e2e/offline.mts` 10/10 + ship-gate row `offline.coldStartPlayable` |
| 59 | "30 min tasks lol?" → "do 1-5 with subagents" | **done** | (1) `fbbcf5f` dist 73.23 → 46.34 MB, 229 → 201 files (26.89 MB of flat model copies nothing requested, also double-counted in the load manifest); (2/3/5) one tier per device + no 2x art on 1x + `og.jpg` out of the pack — first boot 39.01 → 37.08 MB on a DPR-2 phone, → 35.73 MB on a 1x desktop, denominator moving in step, cache after one load 180 → 164 entries; (4) `411697e` the Canyon is back in the phone opening frame (zoom 0.87 → 0.75 at 932×430, desktop bit-identical). Flagged: the boot suite's stuck detector is calibrated against the pre-ask-58 denominator |
| 60 | (parent, from 59) the boot e2e's B3 stuck-detector is calibrated against the pre-ask-58 27 MB denominator — 1 % is now 378 KB, so a window that saw 153 KB of movement arithmetically cannot tick and the row flaps (3/4 then 2/4 with different rows) | **queued** | re-calibrate the detector against the declared denominator, or measure bytes rather than percent |
| 63 | "Make a 15s trailer of the latest build / And show off all the new UI we did" -> (r2) "lol bro the trailer shows no gameplay it should be at least 50% wheelies and jumps and backflips" | **done (r2)** | r1 was all front end. r2 is **58 % riding**: 2.52 s rooftop wheelie (h1 Pro), 2.33 s / 10.5 m air (x3 Pro), a real **349 deg backflip** landed off the x1 summit at 15.1 m, the foundry fire line, e1s canyon gap, the b1 finish - world map + garage keep one bar between them. **No golden contained a flip** (survey.ts: max rotation anywhere 40 deg, the bot rides for time), so `make-flip.ts` sweeps 972 lean schedules off a golden takeoff and keeps the furthest that lands: 405 land clean, winner 349 deg (wildest 543 deg, crashes); recording committed at `harness/inputs/x1-vertical-limit/trailer-flip.json`, replays to 0 faults. Two bugs in that tool first: the search settle (0.4 s) was shorter than the recorded tail (2 s) so loop-outs scored as landings, and recordings quantise lean to i8 so a raw -0.8 simulated differently than it replays (-0.80315) - both now share one routine through `quantizeInput`. survey.ts gained WHEELIE + net-rotation/FLIP measurement. Capture: `capture-ui.ts` drives the live front end on a *paused* fake clock (one `clock.runFor` per output frame, so a 3 fps SwiftShader page yields a deterministic 30 fps clip). Cut list `harness/trailer/shots-ui.md`; outputs gitignored (HR-11) |
| 64 | (parent, from 63) trailer round 3 — the options round 2 left on the table | **queued (blocked on HR-12)** | Waiting on the human watching the cut. Startable once they answer: (a) retime whichever beat they name — the cut list is `harness/trailer/shots-ui.md`, every beat is a half-bar on the 124 BPM grid, re-render is `capture-ui.sh <sha7>`; (b) swap the round-1 phone-in-a-bezel shot back in for an air beat (`shots-ui.json` still scripts `menu`/`mapcard`/`phone`; `edit.py` has the `phonecard` card); (c) the unused money shot — `make-flip.ts` found a **543 deg** flip-and-a-half off the same x1 takeoff that *crashes*, which is the v0.2.0 cut's "every crash is a restart" beat with a far better trick in it (`--takeoff 43.0`, delay=0 hold=240 lean=-1 thr=1). **Also found and ruled, not a defect:** on desktop the world map focuses whatever marker the pointer moves over, so a single mouse click on a marker launches that track immediately (`worldMapScreen.ts` pointermove + click `already`). That reads as deliberate — hover shows the card, click rides — but it means map automation must not click markers (it launched B3 mid-capture); use `ArrowLeft`/`ArrowRight` or click open land while zoomed out |
| 65 | "What plan files do we have in the repo" | **done** | Listed the seven files in `docs/plans/` and distinguished the archived plans in `project/archive/` on 2026-09-21. |
| 66 | "Can you commit and push the work here?" | **done** | Rewritten `main` plus the PWA archive/status commit pushed to `origin/main` on 2026-09-21. Generated working-tree output remains local: a 2-seed reflex run replacing tracked 9-seed evidence, a partial gate JSON, and regenerable world-map source PNGs. |
| 67 | "Is PWA offline completed and can it be archived?" → "I checked the PWA offline it works" | **done** | Headless origin-down gate proves one-load cold start, exact B1 finish, garage swaps, and slow-link warm start; the user confirmed the PWA works offline on the actual phone on 2026-09-21. Plan archived at `project/archive/PWA_OFFLINE.md`, tracker updated; closure tag `pwa-offline-complete`. |
| 68 | Rewrite history to remove the >100 MB video, then push older commits in batches of 25 until current | **done** | Rewrote `harness/out/trailer/trailer-v0.2.0.mp4` (141 657 181 B) out of history; the rewritten tip tree differs only by that file. Uploaded 345 historical commits to `history-repair-upload` in 25-commit batches, except a merge/branch boundary, the final remainder, and two smaller art splits required by GitHub's 2 GiB pack cap; verified each remote tip, then moved `origin/main` to the full rewritten history. |
| 69 | "Can we commit and push what we have locally 35 files" | **done** | Committed all 35 pre-existing local files: 24 reflex JSONs plus `reflex.md` (a dated two-seed run, narrower than the previous nine-seed baseline), nine generated world-map source PNGs, and one partial offline gate JSON. Validated the JSON/PNG files and pushed the commit to `origin/main`. |
| 70 | "Are we going to look at the other docs plans; are any complete and ready to archive?" | **done** | Audited the live plans against their done lines and archive rule: none is ready. World map has seven machine-evidenced lines complete but needs an actual iPhone gesture reading (HR-13); hero integration has three of five acceptance groups complete, with the phone-high bar missed and the iPhone garage reading open (HR-10). Perf backlog, riding poses, and physics-library plan remain open. Corrected the tracker and queued the device reading. |
| 71 | "I mean both of plants are down where you can archive them." | **done** | User accepted closure of the world map and hero art integration plans. Both moved to `project/archive/` with closure pins at the top and tracker rows marked done. The unmeasured iPhone checks remain explicit exceptions; the measured hero phone-high gap moved to `docs/plans/PERF-BACKLOG.md`. |

## 2026-09-21 — Native app publishing

| # | ask (the user's words, shortened) | status | owner / evidence |
|---|---|---|---|
| 72 | Create a new branch and plan native iOS / Android browser-wrapper apps, keeping TypeScript + Three.js; explain whether website updates can reach installed apps without store updates | **done** | Branch `docs/native-mobile-publishing`; [`NATIVE_MOBILE_PUBLISHING.md`](../plans/NATIVE_MOBILE_PUBLISHING.md) covers Capacitor, repo changes, store submission, costs, device gates and optional OTA with fallback; official policy sources checked 2026-09-21. Plan delivered; implementation authorized separately in ask 73; publication prerequisites HR-14. |
| 73 | “Windows plans are complete, and we're blessing to build them autonomously.” | **in flight** | Parent — user confirmed iOS and Android only; autonomous implementation authorized, Capacitor shells, native boot/lifecycle/durable saves and signed OTA implemented; iOS/Android local staging, activation and watchdog rollback proved. Repeatable OTA qualification passes 17 iOS stages and 18 Android checks, including interrupted retry and startup recovery with saved preferences. Production channel configuration, low-storage/binary-upgrade and remaining device gates, plus account/store steps remain open (HR-14). |
| 74 | “Any questions or decisions in the plan? It’s iOS and android only” | **done** | Windows excluded; publisher identity, first-release monetization and update strategy asked while implementation continues. Technical default is Capacitor with offline bundled assets. |
| 75 | How will remote updates work; can the Vercel web game / PWA also implement them? | **done** | Explained existing PWA service-worker updates versus proposed verified native bundles, both hosted on Vercel with independent mobile promotion; eligible OTA now required in first native release. |
| 76 | Which iOS/Android simulators are available for end-to-end testing; add them to the plan and goal | **done** | Live inventory captured in `docs/evidence/native-mobile/simulators.json`; iOS26.5 phone/tablet matrix and Pixel7/API36 full suites added as explicit active-goal acceptance gates. Available versus tested and older-OS gaps recorded; executing the matrix remains part of ask73. |
| 77 | (parent, from native E2E) all-track sweep logs mixed-index geometry merge errors and drops scenery | **done** | Identity-indexed extrusions restore rust-steel obstacle meshes on six tracks without changing vertex/normal/UV data. All24track construction regressions pass; installed iOS and Android asset probes pass24tracks/5outfits/2bikes with zero captured errors. Before/after: `docs/evidence/native-mobile/ios-capabilities-before.json`, `ios-capabilities.json`, `android-capabilities.json`. |
| 78 | (parent, from native E2E) outer touch-control labels cross iOS landscape safe-area margins | **done** | Outer visual keys now respect horizontal safe insets; full-quarter gestures preserved. Five installed iOS profiles pass measured bounds/delegated hit tests: `docs/evidence/native-mobile/ios-touch-matrix.json`; before/after on primary iPhone retained. |
| 79 | (native Android OS touch E2E) quick restart tap between rendered frames is lost | **done** | Completed unsampled taps are consumed once; long holds preserve threshold behavior and release never doubles them. Cancellation/interruption regressions pass; installed Android passes six instantaneous OS restart taps plus held/release and the full navigation/lifecycle/save suite. `docs/evidence/native-mobile/android-touch.json`. |
