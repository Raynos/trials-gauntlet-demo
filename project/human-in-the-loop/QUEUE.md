# Human queue

Things that need **you**, not an agent. Everything else is `docs/tasks/ASKS.md`. Keep it short; **delete a line once it is
decided** — the decision lands in the thing it changed (the plan, the SPEC, the ASKS row). Each line leads with what it is
waiting on. IDs never reused. Ported from the FF15 demo's `HUMAN_REVIEW.md`.

- **HR-05 — First review-inbox note.** Waiting on: you — when the ✎ NOTE button ships, enter the password once and send one note;
  I pull it and the loop is proven.

- **HR-09 — Astra's hero art, now in the game.** Waiting on: you — play it on your iPhone (menu → GARAGE: cycle the five outfits and both bikes; then b1): is the garage at 30 fps, and is the hair right? The strand groom could not ship (3.46 M tris); it is a baked curl shell — compare `docs/evidence/hero-art/hair-options/{a-delivered-groom,d-shell-v2}.png` and say keep / redo. Also the **licence call before any public deploy**: the beard pack says CC-BY in its README but AGPL in its files, the curls are Bystedt CC BY-SA (`docs/evidence/hero-art/delivery/provenance/`); trunk is not pinned in `RELEASES.md` until you clear that. Played evidence: `docs/evidence/hero-art/round1/` and `round4/`.
