# Human queue

Convention: [README.md](README.md). Agents append; the user answers inline or in chat; answered items are struck through and dated.

## Decisions (HD-nn)

- **HD-01 — Level-select direction.** Waiting on: you — pick from `assets/design/tracks/round2/` (A1 / A2 / A3 / A+D / A+E, contact
  sheet + SPEC) once it lands. You already ruled *A biome map* as the base; this settles the variant. Default applied: none — the
  build is parked until you pick. Reversal: the build has not started.

## Reviews (HR-nn)

- **HR-01 — Garage direction.** Waiting on: you — `assets/design/garage/round2/` (BE1 / BE2 / BE3: B's UI × E's set) when it lands.
  Answered so far: UI layout = **B tool wall**, set = **E shutter door** (2026-09-16); the builder is on that. This item settles which BE variant.
- **HR-02 — Is the v0.2.1 hero good?** Waiting on: you — `docs/evidence/hero-delta-v010-v021/` (six sheets + two turntable videos).
  The numbers say the rider is 4× the geometry with a real head and three outfits and the bike barely changed; the face is the weak
  point (the art gate `docs/plans/HERO_GARAGE_PRODUCTION.md` is paused on it). Verdict wanted: keep / re-open the art route / park.
- **HR-03 — img2 fix on the phone.** Waiting on: you — open https://trials-gauntlet-demo.vercel.app on iOS Safari, garage → Img2
  experiment → play b1. Headless Chromium + WebKit render it (`b06f9ed`); a real-device confirmation closes it.
- **HR-04 — The garage rebuild, played.** Waiting on: you — when the garage round commits, play it on the phone: hero unobstructed,
  drag-rotate, pinch-zoom, selectors gone from main + pause menus. Verdict: ship / fix list.

## Answered

_(none yet)_
