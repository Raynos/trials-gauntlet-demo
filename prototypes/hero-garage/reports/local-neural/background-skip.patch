diff --git a/scripts/generate_asset.py b/scripts/generate_asset.py
index b6be4c0..29b396f 100755
--- a/scripts/generate_asset.py
+++ b/scripts/generate_asset.py
@@ -419,6 +419,7 @@ def _load_pipeline(args):
         revision=TRELLIS_REVISION,
         cache_dir=args.cache_dir,
         local_files_only=args.offline,
+        load_background_model=args.background != "keep",
     )
     if args.backend == "mps":
         pipeline.to(torch.device("mps"))
@@ -534,7 +535,7 @@ def main() -> int:
             TRELLIS_REPO: TRELLIS_REVISION,
             TRELLIS_IMAGE_LARGE_REPO: TRELLIS_IMAGE_LARGE_REVISION,
             DINOV3_REPO: DINOV3_REVISION,
-            RMBG_REPO: RMBG_REVISION,
+            **({RMBG_REPO: RMBG_REVISION} if args.background != "keep" else {}),
         },
         "source_revisions": SOURCE_REVISIONS,
         "platform": {
diff --git a/trellis2/pipelines/trellis2_image_to_3d.py b/trellis2/pipelines/trellis2_image_to_3d.py
index a6d25ef..35c9f76 100644
--- a/trellis2/pipelines/trellis2_image_to_3d.py
+++ b/trellis2/pipelines/trellis2_image_to_3d.py
@@ -93,6 +93,7 @@ class Trellis2ImageTo3DPipeline(Pipeline):
         cls,
         path: str,
         config_file: str = "pipeline.json",
+        load_background_model: bool = True,
         **hub_kwargs,
     ) -> "Trellis2ImageTo3DPipeline":
         """
@@ -124,10 +125,12 @@ class Trellis2ImageTo3DPipeline(Pipeline):
             **args['image_cond_model']['args'],
             **dependency_hub_kwargs,
         )
-        pipeline.rembg_model = getattr(rembg, args['rembg_model']['name'])(
-            **args['rembg_model']['args'],
-            **dependency_hub_kwargs,
-        )
+        pipeline.rembg_model = None
+        if load_background_model:
+            pipeline.rembg_model = getattr(rembg, args['rembg_model']['name'])(
+                **args['rembg_model']['args'],
+                **dependency_hub_kwargs,
+            )
         
         pipeline.low_vram = args.get('low_vram', True)
         pipeline.default_pipeline_type = args.get('default_pipeline_type', '1024_cascade')
